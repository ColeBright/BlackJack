@using BlackJack.Helpers
@model BlackJack.Domain.GameModels.Card

@{
    var imgPath = Url.Content(CardImageHelper.GetCardImagePath(Model));
}

<img src="@imgPath" alt="Model" style="width: 100px; height:auto;" />
